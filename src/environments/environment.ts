export const environment = {
    production: false,
    apiUrl: 'http://localhost:3000',
    guestData: { userName: 'guest', passWord: 'guest' }
};


// import 'zone.js/dist/zone-error';  // Included with Angular CLI.